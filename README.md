# AI RAG Chatbot (FastAPI)

This project is a minimal Retrieval-Augmented Generation (RAG) chatbot built with:

- FastAPI
- ChromaDB
- Docker / Docker Compose

## Architecture

Client -> FastAPI -> ChromaDB -> LLM

## Getting Started

```bash
docker compose up --build
